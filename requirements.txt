# Runtime dependencies for deployment targets that don't use uv.lock
# (e.g. Streamlit Community Cloud, which installs from this file).
# For local development use the lockfile instead: `uv sync --all-extras`.
httpx>=0.27
trafilatura>=1.8
ddgs>=9.0
pypdf>=4.0
Markdown>=3.7
bleach>=6.2
streamlit>=1.30
fpdf2>=2.7
python-docx>=1.1
